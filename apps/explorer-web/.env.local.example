# Database (PostgreSQL + TimescaleDB)
DATABASE_URL=postgresql://explorer:explorer_dev@localhost:54320/stellar_explorer?sslmode=disable

# Redis
REDIS_URL=redis://localhost:63790

# Typesense
TYPESENSE_URL=http://localhost:18108
TYPESENSE_API_KEY=explorer_dev_key

# Stellar RPC (for contract simulation, live state queries)
NEXT_PUBLIC_RPC_URL=https://soroban-rpc.mainnet.stellar.gateway.fm

# Public app URL (used for absolute links in Copy for AI, exports, etc.)
NEXT_PUBLIC_APP_URL=https://stellarview.acachete.xyz

# Umami analytics website ID (optional — when unset, Umami script is not injected)
NEXT_PUBLIC_UMAMI_WEBSITE_ID=

# Indexer API URL for network-wide analytics (optional — when unset, analytics charts show "not available yet")
NEXT_PUBLIC_INDEXER_URL=
